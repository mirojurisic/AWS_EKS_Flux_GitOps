apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-app.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "flask-app.name" . }}
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
